{
  "bsonType": "object",
  "required": ["userId", "pointId", "createTime"],
  "permission": {
    "read": "doc.userId == auth.openid || auth.role == 'admin'",
    "create": "auth != null",
    "delete": "doc.userId == auth.openid || auth.role == 'admin'"
  },
  "properties": {
    "_id": {
      "description": "系统自动生成的唯一ID"
    },
    "userId": {
      "bsonType": "string",
      "description": "用户ID",
      "default": { "$env": "auth.openid" }
    },
    "pointId": {
      "bsonType": "string",
      "description": "被收藏的点位ID",
      "minLength": 1
    },
    "createTime": {
      "bsonType": "date",
      "description": "收藏时间",
      "default": { "$env": "now" }
    }
  },
  "indexes": [
    { 
      "keys": { "userId": 1, "pointId": 1 }, 
      "options": { "name": "idx_user_point", "unique": true } 
    }
  ]
}